# Mobile App

The canonical mobile app documentation is in
[../docs/platforms/mobile.md](../docs/platforms/mobile.md).

Useful commands from this directory:

```bash
npm start
npm run ios
npm run android
npm run typecheck
npm test
```

Native wallet flows require a development build because crypto uses native
modules.
